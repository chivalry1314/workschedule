# 排班管理系统产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | 排班管理系统 |
| 文档版本 | v1.0.0 |
| 编写日期 | 2025-01-16 |
| 目标读者 | 产品经理、开发工程师、测试工程师、UI设计师 |
| 状态 | 初稿 |

---

## 2. 项目概述

### 2.1 项目背景
为满足组织内部人员值班安排的统一管理需求，需要一个支持角色、值班类型、月度排班及换班申请的在线系统。系统通过管理员配置基础数据，普通人员自主填报与协商换班，实现排班工作的透明化与自动化。

### 2.2 产品目标
- 提供一个覆盖「人员管理—角色管理—值班类型管理—月度排班—换班申请—排班查看」的闭环系统。
- 降低人工排班沟通成本，确保排班数据统一、可追溯。
- 支持按人员视角和管理员视角分别查看排班结果。

### 2.3 产品定位
面向中小型团队/组织的内部值班排班工具。系统优先以 **Web 应用** 形式交付，并**重点适配手机浏览器访问**，保证普通人员可通过手机完成排班填报、换班申请、查看排班等高频操作。桌面端主要服务于管理员的配置与总排班查看工作。

---

## 3. 用户角色与权限

### 3.1 角色定义

| 角色 | 权限范围 |
|------|----------|
| 管理员 | 人员管理、角色管理、值班类型管理、查看所有人排班、系统设置 |
| 普通人员 | 填写个人值班计划、发起/处理换班申请、查看总排班、查看个人排班 |

### 3.2 权限矩阵

| 功能模块 | 管理员 | 普通人员 |
|----------|--------|----------|
| 人员管理 | ✅ | ❌ |
| 角色管理 | ✅ | ❌ |
| 值班类型管理 | ✅ | ❌ |
| 填写个人值班计划 | ✅ | ✅ |
| 发起换班申请 | ✅ | ✅ |
| 处理换班申请（同意/拒绝） | ✅ | ✅ |
| 查看总排班计划 | ✅ | ✅ |
| 查看个人排班计划 | ✅ | ✅ |

> 说明：管理员同样具备普通人员的核心操作权限，因此管理员也可以填写自己的排班计划并发起换班。

---

## 4. 功能需求

### 4.1 登录模块

#### 4.1.1 登录方式
- 使用「用户名 + 密码」登录。
- 用户名由管理员创建人员时设置，不可重复。
- 初始密码由管理员设置，首次登录后系统**强制要求修改密码**。

#### 4.1.2 首次登录强制改密
- 首次登录成功后，跳转至「修改密码」页面。
- 新密码需满足：长度 ≥ 8 位，且同时包含字母与数字。
- 未完成改密前，不能进入系统其他页面（退出登录除外）。

#### 4.1.3 密码修改/重置
- 普通人员可在「个人设置」中修改密码。
- 若忘记密码，需联系管理员重置为新的初始密码；重置后再次登录时强制改密。

---

### 4.2 人员管理模块（管理员）

#### 4.2.1 添加人员
- 字段：姓名、用户名（唯一）、初始密码、所属角色、手机号（可选）、备注（可选）。
- 用户名不可重复，系统校验唯一性。
- 新增成功后，人员可立即使用用户名与初始密码登录。

#### 4.2.2 编辑人员
- 可修改：姓名、所属角色、手机号、备注、账户启用/禁用状态。
- 不可修改：用户名（作为系统唯一标识）。
- 修改角色后，不影响历史排班数据；仅影响后续新建排班时的可选值班类型。

#### 4.2.3 删除人员
- 支持删除人员，但**已存在排班或换班记录的人员不可直接删除**，需先转移或处理历史数据。
- 删除前进行二次确认。

#### 4.2.4 重置密码
- 管理员可为人员重置密码，重置后生成新的初始密码并提示管理员告知用户。
- 重置密码后，该人员下次登录强制改密。

#### 4.2.5 人员列表
- 列表字段：姓名、用户名、角色、状态（启用/禁用）、手机号、创建时间、操作。
- 支持按姓名/用户名搜索。
- 支持按角色筛选。

---

### 4.3 角色管理模块（管理员）

#### 4.3.1 添加角色
- 字段：角色名称、关联值班类型（多选）、备注。
- 角色名称唯一。
- 关联的值班类型决定该角色下的人员在填写值班计划时可选择的类型。

#### 4.3.2 编辑角色
- 可修改角色名称、关联值班类型、备注。
- 修改关联值班类型后，已填写排班中不合法的值班类型继续保留显示但标记为「已失效类型」，新建排班时仅显示当前关联类型。

#### 4.3.3 删除角色
- 若角色下存在人员，则不可删除，提示先转移人员。

#### 4.3.4 角色列表
- 列表字段：角色名称、关联值班类型数量、人员数量、备注、操作。
- 支持按角色名称搜索。

---

### 4.4 值班类型管理模块（管理员）

#### 4.4.1 添加值班类型
- 字段：类型名称、类型编码（唯一）、颜色标识、时间段（可选，如 09:00-18:00）、备注。
- 颜色用于在日历视图中区分不同类型。
- 类型编码用于系统内部标识，建议支持字母/数字/短横线。

#### 4.4.2 编辑值班类型
- 可修改名称、颜色、时间段、备注。
- 类型编码不可修改。
- 修改颜色后，历史排班显示同步更新颜色。

#### 4.4.3 删除值班类型
- 若该类型已被使用（存在排班记录），则不可删除，可禁用。
- 支持「启用/禁用」切换：禁用后新建排班不可选择，历史记录仍保留显示。

#### 4.4.4 值班类型列表
- 列表字段：类型名称、类型编码、颜色、时间段、状态、操作。
- 支持按名称/编码搜索。

---

### 4.5 月度排班填报模块

#### 4.5.1 排班日历视图
- 以月历形式展示，每一天为一个格子。
- 支持切换年份/月份。
- 日历格子上显示当天已选择的值班类型名称或颜色块。

#### 4.5.2 填写值班计划
- 人员进入「我的排班」页面，选择年份/月份。
- 点击某一天，弹出选择框，显示该人员所属角色关联的值班类型（单选）。
- 选择后保存；同一天不可重复选择多个值班类型，再次选择会覆盖。
- 支持批量选择：按住 Shift/拖拽多天一次性设置相同的值班类型。
- 支持清空某天的排班（设置为「休息/无排班」）。

#### 4.5.3 排班状态
- 每个月的排班存在状态：
  - **草稿**：人员可自行编辑；
  - **已提交**：人员确认后提交，提交后个人不可修改（需由管理员驳回或由换班流程变更）；
  - **已锁定**：管理员确认后锁定，锁定后任何人员不可修改，仅可通过换班申请调整。
- 状态流转：草稿 → 已提交 → 已锁定。
- 管理员可操作：驳回（已提交 → 草稿）、锁定（已提交 → 已锁定）、解锁（已锁定 → 已提交/草稿）。

#### 4.5.4 填报截止时间
- 管理员可设置每月排班的填报截止时间（全局设置或按月设置）。
- 超过截止时间后，人员不可再填写/修改该月排班（除非管理员解锁）。

---

### 4.6 换班申请模块

#### 4.6.1 发起换班申请
- 人员在「我的排班」或「总排班」页面，选择自己的某一天与目标人员（可选）的某一天，发起换班申请。
- 换班类型分为：
  - **指定对象换班**：申请人选择具体的被换班人员及日期；
  - **公开换班**：申请人发布换班需求，其他人员可主动认领。
- 填写说明：换班原因（必填，至少 5 个字符）。
- 申请发起后，对应日期的排班进入「换班中」临时状态，原排班仍可见但不可再发起其他换班。

#### 4.6.2 处理换班申请
- 被换班人员收到申请通知（站内通知），可在「换班申请」列表中查看。
- 操作：同意 / 拒绝。
- 同意后，系统交换两人对应日期的排班类型，并记录换班历史。
- 拒绝后，申请关闭，原排班恢复可编辑/可换班状态。
- 申请人可主动撤回未处理的申请。

#### 4.6.3 换班规则
- 只能交换两人对应日期的排班，不能单方向转让。
- 被换班人员必须当日已有排班（不能是休息）。
- 换班后两人的值班类型会互换，需保证互换后仍属于各自角色允许的值班类型范围。
- 已锁定的月份，换班申请需额外管理员审批；未锁定月份，双方同意即可生效。
- 同一天同一人员只能存在一笔进行中的换班申请。

#### 4.6.4 换班记录
- 记录字段：申请人、被申请人、申请日期、原排班、目标排班、状态、处理时间。
- 管理员与参与双方均可查看相关记录。

---

### 4.7 排班查看模块

#### 4.7.1 总排班计划
- 以月历或表格形式展示全部人员的排班。
- 支持按角色、人员、值班类型筛选。
- 支持导出为 Excel / PDF（后续版本支持）。
- 点击某一天，可查看当日所有值班人员明细。

#### 4.7.2 个人排班计划
- 以月历形式展示当前登录人员的排班。
- 支持切换年份/月份。
- 区分可编辑状态与只读状态（已提交/已锁定）。
- 在排班详情中标识是否由换班产生。

---

## 5. 非功能需求

### 5.1 性能需求
- 页面首次加载时间 ≤ 2 秒（正常网络）。
- 月历排班数据查询 ≤ 500ms。
- 支持同时在线用户数 ≥ 200。

### 5.2 兼容性需求
- 支持主流浏览器：Chrome、Edge、Firefox、Safari 最新两个主版本。
- **重点适配手机 Web 端**：页面需采用响应式布局，保证在手机浏览器（iOS Safari、Android Chrome）下可正常访问与操作。
- 手机端交互要求：
  - 登录页、我的排班、换班申请、个人排班查看等高频页面需优先保证移动端可用。
  - 月历视图在手机屏幕上需支持横向滑动切换月份、点击日期弹窗选择值班类型。
  - 表单输入框、按钮、日期选择器需适配触控操作，避免误触。
  - 底部固定导航或汉堡菜单，方便手机端切换功能模块。
- 桌面端主要服务于管理员的批量配置与总排班查看。

### 5.3 安全需求
- 密码使用 bcrypt/argon2 等不可逆加密方式存储。
- 接口需做身份认证与权限校验，防止越权访问。
- 敏感操作（删除、重置密码、锁定排班）需二次确认。
- 接口防暴力破解：登录失败 5 次后锁定账户 30 分钟。

### 5.4 可用性需求
- 操作后即时反馈（Toast/消息提示）。
- 关键表单需有前端校验与后端校验双重保障。
- 错误提示文案友好，避免暴露系统内部错误信息。

### 5.5 数据备份需求
- 数据库每日自动备份（后续由运维侧配置）。
- 支持手动导出排班数据。

---

## 6. 数据模型设计

### 6.1 用户表（users）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| username | VARCHAR(64) UNIQUE | 登录用户名 |
| password_hash | VARCHAR(255) | 密码哈希 |
| real_name | VARCHAR(64) | 真实姓名 |
| role_id | BIGINT FK | 所属角色 |
| phone | VARCHAR(20) | 手机号，可选 |
| remark | VARCHAR(255) | 备注 |
| is_admin | BOOLEAN | 是否管理员 |
| status | TINYINT | 0-禁用，1-启用 |
| first_login | BOOLEAN | 是否首次登录，默认 true |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 6.2 角色表（roles）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| name | VARCHAR(64) UNIQUE | 角色名称 |
| remark | VARCHAR(255) | 备注 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 6.3 角色值班类型关联表（role_shift_type）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| role_id | BIGINT FK | 角色 ID |
| shift_type_id | BIGINT FK | 值班类型 ID |

### 6.4 值班类型表（shift_types）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| name | VARCHAR(64) | 类型名称 |
| code | VARCHAR(64) UNIQUE | 类型编码 |
| color | VARCHAR(20) | 颜色标识，如 #FF5733 |
| time_range | VARCHAR(64) | 时间段，可选 |
| remark | VARCHAR(255) | 备注 |
| status | TINYINT | 0-禁用，1-启用 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 6.5 排班表（schedules）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| user_id | BIGINT FK | 人员 ID |
| shift_type_id | BIGINT FK | 值班类型 ID |
| work_date | DATE | 值班日期 |
| month_key | VARCHAR(7) | 年月，格式 YYYY-MM，用于快速查询 |
| status | TINYINT | 0-草稿，1-已提交，2-已锁定 |
| source | TINYINT | 来源：0-手动填报，1-换班 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| UNIQUE(user_id, work_date) | | 同一人员同一天唯一 |

### 6.6 换班申请表（shift_swaps）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| applicant_id | BIGINT FK | 申请人 |
| applicant_schedule_id | BIGINT FK | 申请人原排班 |
| target_user_id | BIGINT FK | 被换班人（指定对象时必填） |
| target_schedule_id | BIGINT FK | 被换班人原排班 |
| swap_type | TINYINT | 0-指定对象换班，1-公开换班 |
| reason | VARCHAR(500) | 换班原因 |
| status | TINYINT | 0-待处理，1-同意，2-拒绝，3-撤回，4-管理员审批中 |
| admin_approved | BOOLEAN | 管理员是否已审批（锁定月份） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| resolved_at | DATETIME | 处理时间 |

### 6.7 系统设置表（settings）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| setting_key | VARCHAR(128) UNIQUE | 设置项键 |
| setting_value | TEXT | 设置项值 |
| remark | VARCHAR(255) | 说明 |

---

## 7. 页面结构与导航

### 7.1 登录页
- 用户名输入框
- 密码输入框
- 登录按钮
- 忘记密码提示（联系管理员）

### 7.2 首次改密页
- 原密码（初始密码）
- 新密码
- 确认新密码
- 提交按钮

### 7.3 主布局
- 桌面端：
  - 顶部：系统名称、当前登录人、消息通知、退出登录
  - 左侧导航菜单：
    - 首页 / 仪表盘
    - 我的排班
    - 总排班
    - 换班申请
    - 个人设置
    - 管理员区域（管理员可见）：
- 手机 Web 端：
  - 顶部简化标题栏，左侧为汉堡菜单入口。
  - 底部固定 Tab 导航：首页、我的排班、总排班、换班申请、我的。
  - 管理员功能入口收纳至「我的 → 管理员区域」中，避免底部 Tab 过多。
  - 所有弹窗、选择器、表单需适配小屏幕，必要时采用底部上滑面板（Bottom Sheet）替代居中弹窗。
    - 人员管理
    - 角色管理
    - 值班类型管理
    - 系统设置

### 7.4 页面详情

#### 7.4.1 首页/仪表盘
- 展示本月我的排班统计（各类型天数）。
- 展示待处理换班申请数量。
- 快捷入口：填写本月排班、查看总排班。

#### 7.4.2 我的排班页
- 年份/月份选择器。
- 月历视图，每天显示值班类型。
- 状态提示：草稿/已提交/已锁定。
- 操作按钮：提交排班、发起换班。
- 填报截止提示。

#### 7.4.3 总排班页
- 年份/月份选择器。
- 人员筛选、角色筛选、值班类型筛选。
- 月历/列表两种视图切换。
- 导出按钮（占位）。

#### 7.4.4 换班申请页
- Tab：我发起的 / 我收到的 / 历史记录 / 公开换班。
- 列表字段：日期、对方、原类型、目标类型、状态、操作。

#### 7.4.5 人员管理页
- 人员列表表格。
- 新增/编辑/删除/重置密码按钮。

#### 7.4.6 角色管理页
- 角色列表表格。
- 新增/编辑/删除按钮。
- 关联值班类型选择弹窗。

#### 7.4.7 值班类型管理页
- 类型列表表格。
- 新增/编辑/删除/启用禁用按钮。

#### 7.4.8 系统设置页
- 全局填报截止时间设置。
- 密码复杂度设置。
- 系统名称/Logo 设置（可选）。

---

## 8. 核心业务流程

### 8.1 人员创建与首次登录流程
```
管理员添加人员 → 设置初始密码 → 人员首次登录 → 强制修改密码 → 进入系统
```

### 8.2 月度排班填报流程
```
人员选择月份 → 填写/修改每日值班类型 → 确认无误 → 提交排班
            ↓（如需修改）
      管理员驳回 → 回到草稿
            ↓（确认无误）
      管理员锁定 → 排班生效
```

### 8.3 换班流程（未锁定月份）
```
申请人发起换班 → 被申请人收到通知 → 同意 → 系统交换排班 → 换班成功
                                      ↓
                                    拒绝 → 申请关闭
```

### 8.4 换班流程（已锁定月份）
```
申请人发起换班 → 被申请人同意 → 进入管理员审批 → 管理员通过 → 换班成功
                                      ↓
                                    管理员驳回 → 申请关闭
```

---

## 9. 状态定义

### 9.1 账户状态
- 启用：可正常登录使用。
- 禁用：不可登录，不可参与排班。

### 9.2 排班记录状态
- 草稿：可编辑。
- 已提交：个人不可编辑，需管理员操作。
- 已锁定：完全生效，不可手动修改，仅可通过换班调整。

### 9.3 换班申请状态
- 待处理：等待被申请人响应。
- 同意：双方已同意，若月份锁定则需管理员审批。
- 拒绝：被申请人拒绝，申请关闭。
- 撤回：申请人主动撤回，申请关闭。
- 已完成：换班已成功执行，排班已交换。

---

## 10. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 用户名重复 | 提示「用户名已存在」，禁止提交 |
| 角色名称重复 | 提示「角色名称已存在」，禁止提交 |
| 类型编码重复 | 提示「类型编码已存在」，禁止提交 |
| 删除已使用的值班类型 | 提示「该类型已被使用，无法删除，建议禁用」 |
| 删除有人员的角色 | 提示「该角色下存在人员，无法删除」 |
| 删除有排班记录的人员 | 提示「该人员存在排班/换班记录，无法删除」 |
| 非本角色允许的类型 | 后端校验拒绝保存，提示「所选值班类型不在角色允许范围内」 |
| 已锁定月份手动修改 | 提示「该月份排班已锁定，不可修改」 |
| 超填报截止时间 | 提示「已超过本月填报截止时间，请联系管理员」 |

---

## 11. 界面原型建议

### 11.1 我的排班页（月历视图）
```
[2025] [1月]                [提交排班]  [发起换班]

日    一    二    三    四    五    六
      1     2     3     4     5     6
白班  夜班  休息  白班  白班  休息  休息
```
- 每天格子内显示值班类型名称，背景使用类型颜色。
- 选中某天，右侧或弹窗显示详情与操作。

### 11.2 总排班页（列表视图）
```
人员    | 1日 | 2日 | 3日 | ... | 31日
--------|-----|-----|-----|-----|------
张三    | 白班| 夜班| 休息| ... | 白班
李四    | 休息| 白班| 白班| ... | 夜班
```

### 11.3 换班申请弹窗
- 申请人侧：选择自己的日期与当前排班。
- 对方侧：选择目标人员与日期。
- 换班原因输入框。
- 提交按钮。

### 11.4 手机 Web 端适配要点
- 首页 / 我的排班：
  - 月历采用纵向滚动卡片或横向滑动周视图，避免桌面端大月历在小屏挤压。
  - 每一天点击后从底部弹出选择面板，显示所属角色允许的值班类型。
  - 顶部显示当前月份，左右箭头或滑动手势切换月份。
- 总排班：
  - 默认按人员列表展示，点击人员展开该月排班卡片。
  - 提供「只看某日」入口，按日期筛选显示当天所有值班人员。
- 换班申请：
  - 分步骤引导：先选自己的日期 → 再选对方人员与日期 → 填写原因 → 确认提交。
  - 使用底部上滑面板展示人员选择器，避免小屏幕下拉选择困难。
- 通用：
  - 按钮最小触控区域 ≥ 44×44px。
  - 表单输入框字体大小 ≥ 16px，防止 iOS 浏览器缩放输入框。
  - 关键操作（提交排班、确认换班）使用全屏确认或底部固定操作栏。

---

## 12. 后续迭代规划

| 版本 | 规划内容 |
|------|----------|
| v1.0 | 基础功能 + 手机 Web 端优先适配 |
| v1.1 | 排班数据导出 Excel；排班统计报表 |
| v1.2 | 消息通知（邮件/短信/站内信）提醒换班与截止 |
| v1.3 | PWA 离线缓存支持；微信小程序接入 |
| v1.4 | 自动排班算法（按规则自动生成初稿） |
| v1.5 | 多组织/多部门支持；权限细分 |

---

## 13. 附录

### 13.1 术语表

| 术语 | 说明 |
|------|------|
| 排班 | 某个人在某一天需要执行的值班类型安排 |
| 值班类型 | 管理员定义的班次种类，如白班、夜班、休息等 |
| 角色 | 一组人员的分类，可关联多个允许的值班类型 |
| 换班 | 两名人员交换各自某一天排班的过程 |
| 月历视图 | 以日历形式展示排班，按天显示 |
| 锁定 | 管理员确认排班后，禁止普通人员再修改 |

### 13.2 参考与依赖
- 前端：Vue 3 / React 18 + TypeScript（待定）
- 后端：Node.js / Python / Java（待定）
- 数据库：PostgreSQL（Prisma 适配）
- 部署：Docker + Nginx（待定）

---

## 14. 修订记录

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| v1.0.0 | 2025-01-16 | - | 初稿完成 |
| v1.0.1 | 2025-01-16 | - | 强化手机 Web 端适配要求：产品定位、兼容性需求、主布局、界面原型、迭代规划 |
