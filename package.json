{
  "name": "workschedule",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:edgeone": "cd workschedule-web && rm -f package-lock.json && rm -rf node_modules && npm install && if [ \"$(uname -s)\" = \"Linux\" ]; then npm install --no-save @rolldown/binding-linux-x64-gnu; fi && npm run build && cd ../workschedule-api && npm ci && npm run build:edgeone"
  }
}
