{
  "name": "workschedule",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:edgeone": "cd workschedule-web && npm install && npm run build && cd ../workschedule-api && npm install && npm run build:edgeone"
  }
}
